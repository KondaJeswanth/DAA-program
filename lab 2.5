#include <stdio.h>
int T(int n) {
  if (n == 1) {
    return 1;
  } else {
    return T(n - 1) + 1;
  }
}
int main() {
  int n;
  printf("Enter the value of n: ");
  scanf("%d", &n);
  int result = T(n);
  printf("T(n) for n=%d is: %d\n", n, result);
  return 0;
}
